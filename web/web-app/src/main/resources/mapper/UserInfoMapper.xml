<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.WHY.lease.web.app.mapper.UserInfoMapper">

    <update id="change">
        update user_info set avatar_url=#{avatarUrl} where id=#{userId}
    </update>
    <update id="changeName">
        update user_info set nickname=#{name} where id=#{userId}
    </update>
</mapper>
